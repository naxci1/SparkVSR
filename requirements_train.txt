# ============================================================
# SparkVSR — Training requirements
# Install:  pip install -r requirements.txt -r requirements_train.txt
# ============================================================

# Inference deps are in requirements.txt — install that first.

# ---- Distributed training ----
deepspeed>=0.12.0
accelerate>=1.0.0

# ---- Experiment tracking ----
wandb>=0.16.0
tensorboard>=2.14.0
tensorboardX>=2.6.0

# ---- Data loading / augmentation ----
datasets>=2.18.0
lmdb>=1.4.0
webdataset>=0.2.86
scikit-image>=0.21.0
scipy>=1.11.0
Pillow>=10.0.0
matplotlib>=3.7.0

# ---- Additional model / training utilities ----
bitsandbytes>=0.41.0       # 8-bit optimisers
timm>=0.9.0
addict>=2.4.0
einops>=0.7.0

# ---- Optical flow (RAFT) ----
filterpy>=1.4.5

# ---- Quality metrics during training ----
# Pin to a specific commit for reproducibility
-e git+https://github.com/QualityAssessment/DOVER.git@f1ddc96215bc7fbcf8f315c65d47905f339c3419#egg=dover
