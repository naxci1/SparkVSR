# ============================================================
# SparkVSR — Inference requirements (minimal, no training deps)
# Install:  pip install -r requirements.txt
# For training deps see requirements_train.txt
# ============================================================

# ---- PyTorch (CUDA 12.4 – default for RTX 40xx/50xx) ----
# Install manually for your CUDA version if this line fails:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.5.0
torchvision>=0.20.0

# ---- Core diffusion / model libraries ----
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.26.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
tokenizers>=0.19.0
sentencepiece>=0.1.99
peft>=0.10.0

# ---- Video / image I/O ----
Pillow>=10.0.0
opencv-python>=4.8.0
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
decord>=0.6.0

# ---- Utilities ----
numpy>=1.24.0
einops>=0.7.0
tqdm>=4.65.0
packaging>=23.0
filelock>=3.12.0
PyYAML>=6.0

# ---- Optional: quality metrics (used by eval scripts) ----
pyiqa>=0.1.10

# ---- Optional: API keyframe mode ----
fal_client>=0.5.0

# ---- Optional: GGUF conversion ----
# gguf>=0.5.0
